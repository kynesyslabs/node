// INFO Here we have multiple built-in methods to do crosschain communication and execution
// NOTE This is a completion to the multichain folder modules

import * as multichain from "./multichain"

export default class Crosschain {

    constructor() {}

    // TODO Also fix the arguments
    static async swapBetweenChains(fromChain: string, toChain: string) {
        // TODO work on it
    }

}