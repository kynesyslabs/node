// Local augmentations for @kynesyslabs/demosdk for the upgradable_network track.
//
// Concrete types live under `src/features/staking/types.ts` and
// `src/features/networkUpgrade/types.ts`; this file is reserved for
// declaration-merging into SDK modules *once* those types also need to flow
// through the SDK's public surface (e.g. when a new SDK batch is being
// published). See the nomis-augmentations.d.ts pattern for the mechanism.
//
// Intentionally empty for Phase 0 — Phase 0 types are owned locally and only
// move into the SDK via the published SDK Batch 1 update.

export {}
